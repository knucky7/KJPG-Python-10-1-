korda = int(input("Mitu korda soovite reklaamlauset kuvada?: "))
tekst = input("Sisestage reklaamlause: ")
kordusi = 0

def banner(banner):
    print(tekst.upper())
    return tekst.upper()

while kordusi < korda:
    banner(banner)
    kordusi += 1
